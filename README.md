# eldritch-harvester

Autonomous data ingestion and evaluation engine for Works-by-Worrell.

## Overview
This repository manages the pipeline that extracts data from public endpoints, queues it locally, evaluates it using an LLM, and persists results via the Warlock MCP.
