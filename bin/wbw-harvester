#!/usr/bin/env bash
# Principle 1: Zero-Gatekeeper Execution
# Resolves directory context and executes via uv
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$DIR"
echo "Awakening Eldritch Harvester..."
exec uv run python main.py "$@"
